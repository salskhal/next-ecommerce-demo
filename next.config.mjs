/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'via.placeholder.com'], // Add your external image hostnames here
      },
};

export default nextConfig;
